[project]
name = "svunit-tests"
version = "0.0.0"
description = "Pytest harness for the SVUnit framework regression suite."
requires-python = ">=3.10"
dependencies = [
  "pytest>=8.3",
  "pytest-xdist>=3.6",
  "pytest-html>=4.1",
  "pytest-datafiles>=3.0",
]

[tool.uv]
package = false
